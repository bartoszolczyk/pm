
The project can be run in two configurations - (started in IDE/console or on docker completely )

Aat first the project must be build by command - gradle clean build
then it can be launched by docker compose entirely or by launching ony DB and app in IDE or by CLI.


swagger can be found in the link below after start : http://localhost:8080/swagger-ui/#/
JSON file with postman request attached to the root of project: pm.postman_collection.json